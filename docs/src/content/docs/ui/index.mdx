---
title: UI Overview
description: Interactive visualization features in the vizb HTML output.
---

import { CardGrid, Card, Aside } from '@astrojs/starlight/components';

Vizb generates a single self-contained HTML file with a Vue.js-powered interactive UI. No server needed — open the file in any browser.

## Chart Types

Vizb supports four chart types, all rendered in a single view:

<CardGrid>
  <Card title="Bar Chart" icon="bars">
    Default chart type. Compare values across categories. Supports linear and logarithmic scale.
  </Card>
  <Card title="Line Chart" icon="analytics">
    Track trends across X-axis values. Best for sequential data like input sizes or concurrency levels.
  </Card>
  <Card title="Pie Chart" icon="puzzle">
    Show proportional distribution. Useful for comparing relative sizes of benchmark results.
  </Card>
  <Card title="Heatmap" icon="seti:default">
    Fold X, Y, and Z into a colored grid. Cell value sums the z-axis; cell color blends the z palette. See [Heatmap](/ui/heatmap).
  </Card>
</CardGrid>

Control which charts appear with `--charts` (this also controls output file size — see [Output File Size](#output-file-size)):

```bash
vizb bench.txt -c bar,line -o output.html
```

When your data has a z-axis, bar and line charts render in **3D** instead of 2D.
See [3D Charts](/ui/3d-charts).

{/* TODO: Add GIF showing all four chart types rendered side by side */}

## Scale Options

Toggle between linear and logarithmic Y-axis scale:

```bash
# Linear (default)
vizb bench.txt -o output.html

# Logarithmic — better for high-variance data
vizb bench.txt --scale log -o output.html
```

<Aside type="tip">
  Use `--scale log` when benchmark values span several orders of magnitude (e.g., 1 to 1,000,000).
</Aside>

{/* TODO: Add GIF showing linear vs logarithmic scale comparison */}

## Tooltips & Axis Sums

Hovering a data point shows its value alongside **axis-sum** totals. 2D charts
add per-series totals after each series name and a labeled x-marginal at the
bottom of the tooltip. 3D charts add `Σ z` / `Σ x` / `Σ y` marginals — see
[3D Charts](/ui/3d-charts). Tooltips follow light/dark mode automatically.

{/* TODO: Add GIF showing tooltip with axis sums */}

## Fullscreen

Every chart has a **fullscreen** button in its toolbar (next to export). Click it
to expand the chart to the full viewport, and again to exit.

{/* TODO: Add GIF showing fullscreen toggle */}

## Statistics Panel

Each chart has a **statistics** button that opens a panel with a per-series
descriptive table (20 metrics, sortable, searchable, CSV export) and a Pearson /
Spearman **correlation matrix**. Everything is computed off the main thread, so the
UI never blocks. See [Statistics](/ui/stats).

{/* TODO: Add GIF showing the statistics panel */}

## Large Datasets

Vizb stays responsive on big inputs. When the X-axis has more than ~50 categories, a
**dataZoom slider** appears so you can scroll and zoom into a window of the data
(starting zoomed to a readable slice). Datasets above a few thousand points switch to
ECharts' optimized large-data render path, keeping the draw on a single frame.

For CSV/JSON specifically, vizb also **aggregates rows** before charting when
`--group` is set — see [Tabular Data](/guides/data#aggregation).

{/* TODO: Add GIF showing the dataZoom slider on a large dataset */}

## JPEG Export

Export any chart as a JPEG image directly from the UI. Click the export button on any chart to download.

{/* TODO: Add GIF showing JPEG export */}

## Single-File Output

The generated HTML is fully self-contained:
- Vue.js app embedded inline
- Chart data embedded as JSON
- No external dependencies or network requests
- Works offline

Open with any browser — no build step, no server.

## Output File Size

Each chart renderer ships as a separate compressed chunk inside the HTML; only the
ones you select are embedded at generation time.

| Selection | Approx. file size |
|-----------|------------------|
| `bar` only | ~400 kB |
| `bar,line,pie,heatmap` (default, 2D data) | ~445 kB |
| `bar` or `line` with z-axis data | ~570 kB |

The 3D engine (echarts-gl, ~380 kB encoded) is the heaviest component. Vizb includes
it automatically when your data has a z-axis and `bar` or `line` is selected — and
omits it otherwise, even at the default four-chart selection.

To reduce file size, pass `--charts` to bundle only the renderers you need:

```bash
vizb bench.txt -c bar -o output.html       # bar only, ~400 kB
vizb bench.txt -c bar,line -o output.html  # ~415 kB
```

<Aside type="tip">
  For sharing or deploying reports, use `--charts` to include only the charts your
  audience needs. The default bundles all four 2D renderers, which is fine for local
  exploration.
</Aside>

## Next Steps

- [Settings](/ui/settings) — CLI flags and UI settings mapping
- [Axis Swapping](/ui/swapping) — rearrange n/x/y/z dimensions in the UI
- [3D Charts](/ui/3d-charts) — z-axis, auto-rotate, and axis-sum tooltips
- [Heatmap](/ui/heatmap) — fold X/Y/Z into a colored grid
- [Statistics](/ui/stats) — descriptive stats and correlation matrix
- [Output File Size](#output-file-size) — how `--charts` affects HTML size
